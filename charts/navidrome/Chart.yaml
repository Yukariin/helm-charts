apiVersion: v2
name: navidrome
description: A Helm chart for Kubernetes

type: application

version: 0.2.0

# renovate: image=deluan/navidrome
appVersion: "0.57.0"

dependencies:
  - name: common
    version: "0.1.0"
    repository: "file://../common"
